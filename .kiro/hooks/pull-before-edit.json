{
  "version": "v1",
  "hooks": [
    {
      "name": "Git Pull Before Edit",
      "trigger": "PreToolUse",
      "description": "Faz git pull --rebase antes de qualquer edição de arquivo para sincronizar com a Lovable",
      "matcher": "fs_write|str_replace|fs_append",
      "action": {
        "type": "command",
        "command": "git -C \"c:\\Users\\arthu\\saborosamente\" pull origin main --rebase 2>&1"
      }
    }
  ]
}
