{
  "version": "v1",
  "hooks": [
    {
      "name": "Auto Commit & Push on Session End",
      "trigger": "Stop",
      "description": "Faz commit e push único ao final da sessão do Kiro, não a cada save",
      "action": {
        "type": "command",
        "command": "cd \"c:\\Users\\arthu\\saborosamente\" && git add -A && git diff --cached --quiet || git commit -m \"auto: update files\" && git push origin HEAD"
      }
    }
  ]
}
